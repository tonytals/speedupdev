import React from 'react';

const Primeiro = atributos => {
    return (
        <div>
            <h1>{atributos.titulo}</h1>
            <hr></hr>
            <h2>{atributos.sub}</h2>
        </div>
    )
}

export default Primeiro;